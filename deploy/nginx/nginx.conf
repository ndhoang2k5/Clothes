worker_processes  auto;

events {
  worker_connections  1024;
}

http {
  include       /etc/nginx/mime.types;
  default_type  application/octet-stream;

  # Ensure WebP thumbs are served with a correct type (some base images omit this).
  types {
    image/webp webp;
  }

  sendfile        on;
  keepalive_timeout  65;

  # Upload images/files from admin
  client_max_body_size 25m;

  upstream backend_upstream {
    server backend:8000;
  }

  server {
    listen 80;
    server_name _;

    # --- API & static served by backend ---
    location /api/ {
      proxy_pass http://backend_upstream/api/;
      proxy_http_version 1.1;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /static/ {
      proxy_pass http://backend_upstream/static/;
      proxy_http_version 1.1;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
      # Product/upload images: cache at browser & CDN edge for smoother repeat loads
      proxy_hide_header Cache-Control;
      add_header Cache-Control "public, max-age=604800, stale-while-revalidate=86400" always;
      expires 7d;
    }

    # --- Admin SPA under /admin/ ---
    location /admin/ {
      alias /usr/share/nginx/html/admin/;
      try_files $uri $uri/ /admin/index.html;
      add_header Cache-Control "no-store" always;
    }

    # Vite build assets (hashed filenames) — cache long-term
    location /assets/ {
      root /usr/share/nginx/html;
      add_header Cache-Control "public, max-age=31536000, immutable" always;
    }

    # --- Shop SPA at / ---
    location / {
      root /usr/share/nginx/html;
      try_files $uri $uri/ /index.html;
      add_header Cache-Control "no-cache" always;
    }
  }
}

